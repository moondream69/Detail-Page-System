# 家具电商详情页生成器 — 功能现状文档

> 更新时间：2026-04-24

## 项目概述

基于 Next.js 16 + React 19 的单页应用，用户上传家具产品图后，由 AI 视觉模型分析图片并生成结构化文案，前端将文案与图片组合渲染为详情页排版，最终通过 html2canvas 截图导出为 PNG 长图。

## 技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 框架 | Next.js (Turbopack) | 16.2.4 |
| 前端 | React | 19.2.4 |
| 样式 | Tailwind CSS + 自定义 CSS | v4 |
| AI 接口 | OpenAI SDK → api.vectorengine.cn | openai ^6.34.0 |
| AI 模型 | gemini-2.5-flash-lite（多模态视觉） | — |
| 图标 | lucide-react | ^1.9.0 |
| 导出 | html2canvas | ^1.4.1 |

## 文件结构

```
app/
├── page.tsx              # 主页面（配置面板 + 预览区）
├── globals.css           # 全局样式 + 详情页渲染专用类
├── layout.tsx            # 根布局
└── api/
    └── analyze/
        └── route.ts      # AI 分析 API 路由
```

---

## 已实现功能

### 1. 图片上传与管理

- **多图上传**：支持同时上传多张图片，上限 10 张
- **客户端压缩**：上传后自动压缩（最大边 800px，JPEG 质量 0.75），减小传输体积
- **缩略图预览**：上传后以 96×96 缩略图展示
- **单张删除**：hover 显示删除按钮，点击移除对应图片
- **数量计数**：实时显示 `已上传 x/10`

**相关代码**：[page.tsx](app/page.tsx) `compressImage()`、`handleImageUpload()`、`removeImage()`

---

### 2. AI 图片分析（后端）

- **多图联合分析**：将所有上传图片发送给视觉模型，prompt 要求综合分析全部图片
- **动态 prompt 构建**：根据市场配置（平台/市场/语种/风格）和选中模块动态拼接 system prompt
- **模块化 JSON schema**：仅请求用户选中的模块字段，减少无用输出
- **结构化 JSON 输出**：通过 `response_format: { type: "json_object" }` 强制 JSON 返回
- **JSON 解析容错**：自动去除可能的 markdown 标记后重试解析
- **网络兼容**：强制 IPv4 解析（解决代理节点 IPv6 黑洞），设置 `Connection: close` 和 3 次重试

**请求参数**：
```json
{
  "images": ["base64...", "base64..."],
  "platform": "淘宝",
  "market": "中国大陆",
  "language": "中文",
  "visualStyle": "现代简约",
  "modules": ["首屏主视觉", "核心卖点图", ...]
}
```

**返回结构**（按选中模块动态变化）：
```json
{
  "title": "商品主标题",
  "subtitle": "副标题/促销卖点",
  "heroSection": { "tagline": "...", "highlights": ["...", "..."] },
  "sellingPoints": [{ "title": "...", "description": "..." }],
  "usageScenes": [{ "sceneName": "...", "description": "..." }],
  "multiAngle": [{ "angleName": "...", "description": "..." }],
  "atmosphere": [{ "mood": "...", "description": "..." }],
  "details": [{ "detailName": "...", "description": "..." }]
}
```

**相关代码**：[route.ts](app/api/analyze/route.ts)

---

### 3. 市场配置

| 配置项 | 默认值 | 作用 |
|--------|--------|------|
| 电商平台 | 淘宝 | 影响文案调性 |
| 目标市场 | 中国大陆 | 影响受众定位 |
| 文案语种 | 中文 | 决定输出语言 |
| 视觉风格 | （空） | 影响风格描述，默认"现代简约" |

以上四项均为自由输入文本框，值会传入 AI prompt 影响生成结果。

---

### 4. 模块多选

共 6 个可选模块，默认选中前 5 个：

| 模块 | 说明 | 对应字段 | 渲染样式 |
|------|------|----------|----------|
| 首屏主视觉 | 传递核心价值 | `heroSection` | 产品大图 + 标题 + tagline + 亮点标签 |
| 核心卖点图 | 突出卖点优势 | `sellingPoints` | 紫色左边框卡片列表 |
| 使用场景图 | 呈现真实使用场景 | `usageScenes` | 绿色左边框卡片列表 |
| 多角度图 | 多角度呈现外观 | `multiAngle` | 2×2 图片网格 + 角度说明 |
| 场景氛围图 | 展现体验场景 | `atmosphere` | 橙色左边框卡片列表 |
| 商品细节图 | 放大材质工艺 | `details` | 红色左边框卡片列表 |

- 选中的模块会影响 AI prompt（只请求对应字段）
- 预览区只渲染选中模块的内容
- 未选中"首屏主视觉"时仍会显示标题和副标题

---

### 5. 详情页预览与渲染

- **触发**：点击"生成详情页策划方案"按钮
- **渲染容器**：750px 宽白底卡片（`#detail-export-card`），模拟电商详情页比例
- **按模块渲染**：根据 `modules` 数组条件渲染对应板块
- **样式方案**：导出区域使用内联 style（确保 html2canvas 兼容），外围使用 Tailwind

---

### 6. 长图导出

- **技术**：html2canvas
- **流程**：克隆渲染节点到屏幕外 → 2 倍分辨率截图 → 生成 PNG → 触发下载
- **输出格式**：PNG，文件名 `AI-Detail-{timestamp}.png`

**相关代码**：[page.tsx](app/page.tsx) `exportImage()`

---

### 7. 历史记录

- **存储**：localStorage，key 为 `furniture-detail-history`
- **容量**：最多保存 20 条
- **触发保存**：每次 AI 分析成功后自动保存
- **记录内容**：标题、缩略图、时间戳、市场配置、模块选择、AI 分析结果
- **交互**：
  - 点击时钟图标展开/收起历史面板
  - 点击记录项恢复该次的分析结果和配置
  - hover 显示删除按钮

**相关代码**：[page.tsx](app/page.tsx) `saveHistory()`、`restoreHistory()`、`deleteHistoryItem()`

---

## 页面布局

```
┌───────────────────────────────────────────────────────┐
│  左侧配置面板 (420px)    │    右侧预览区 (flex-1)     │
│                          │                            │
│  ┌─ 上传产品图 ──── 🕐 ┐ │  ┌─ AI创作结果 ──────────┐ │
│  │  [图] [图] [+上传]   │ │  │                        │ │
│  │  已上传 2/10         │ │  │   (空状态 / 渲染结果)   │ │
│  └──────────────────────┘ │  │                        │ │
│  ⚠ 提示信息               │  │   [导出长图]            │ │
│                          │  │                        │ │
│  ┌─ 核心卖点 ──── AI编写┐ │  │   ┌──────────────┐    │ │
│  │  (textarea)          │ │  │   │ 750px 详情卡片 │    │ │
│  └──────────────────────┘ │  │   │  按模块渲染    │    │ │
│                          │  │   └──────────────┘    │ │
│  ┌─ 市场配置 ───────────┐ │  │                        │ │
│  │ 平台│市场│语种│风格   │ │  └────────────────────────┘ │
│  └──────────────────────┘ │                            │
│                          │                            │
│  ┌─ 包含模块 ───── 5/6 ─┐ │                            │
│  │ [首屏] [卖点]        │ │                            │
│  │ [场景] [角度]        │ │                            │
│  │ [氛围] [细节]        │ │                            │
│  └──────────────────────┘ │                            │
│                          │                            │
│  ┌──────────────────────┐ │                            │
│  │ [生成详情页策划方案]   │ │                            │
│  └──────────────────────┘ │                            │
└───────────────────────────────────────────────────────┘
```

---

## 数据流

```
用户上传图片
    ↓
客户端压缩 (Canvas, max 800px, JPEG 0.75)
    ↓
点击 "AI编写"
    ↓
POST /api/analyze { images[], platform, market, language, visualStyle, modules[] }
    ↓
后端构建动态 prompt → 调用 gemini-2.5-flash-lite (Vision)
    ↓
返回结构化 JSON → 前端 setFinalResult()
    ↓
自动保存到 localStorage 历史记录
    ↓
点击 "生成详情页策划方案"
    ↓
右侧按模块渲染 HTML 预览
    ↓
点击 "导出长图"
    ↓
html2canvas 截图 → 下载 PNG
```

---

## 当前限制

| 限制 | 说明 |
|------|------|
| 无抠图能力 | 用户图片原样嵌入，不会自动去除背景 |
| 无图片裁切 | 不能自动裁切产品局部细节图 |
| 排版模板固定 | 当前只有一套白底卡片式模板，不可切换 |
| 历史记录不含图片 | 恢复历史记录后，图片区域为空（只恢复文案和配置） |
| html2canvas 限制 | 对复杂 CSS（毛玻璃、阴影叠加）渲染精度有限 |
| 无用户认证 | 无登录系统，数据仅存浏览器本地 |
